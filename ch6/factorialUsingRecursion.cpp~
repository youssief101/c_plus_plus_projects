#include <iostream>
using namespace std;

int factorial(int num) {
  if (num > 1)
    return num - 1 * factorial(num);
  else
    return 1;
}

int main() {
  int val = 5;
  cout << factorial(val) << endl;
  return 0;
}
