#ifndef GLOBALS_H
#define GLOBALS_H

extern const int MAX_USERS;        // maximum number of users program can hold
extern string subscribers_names[]; // array that holds the users names
extern int subscribers_id[];       // array that holds the users IDs
extern int actual_users_size;      // the acutal number of existing users on the system
extern int idValue;

#endif
