#include <iostream>
using namespace std;

void print(int (&arrRef)[], const int size) {
  for (int i = 0; i < size; ++i)
    cout << arrRef[i] << " ";
  cout << endl;
}

int main() {
  int arr[5] = {1, 2, 3, 4, 5};
  print(arr, end(arr) - begin(arr));
  return 0;
}
