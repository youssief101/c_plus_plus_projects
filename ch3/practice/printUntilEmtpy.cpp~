#include <iostream>
#include <vector>
#include <string>
#include <cctype>

using namespace std;

int main() {
  string word;
  vector<string> text;

  while (cin >> word)
    text.push_back(word);

  for (auto iter = text.cbegin();
       iter != text.cend() && !it->empty(); ++iter)
    cout << *iter << endl;
}
